import { Stack } from "@chakra-ui/core"

export const Main = (props) => (
  <Stack
    spacing="1.5rem"
    width="100%"
    py="2rem"
    justifyContent="center"
    minHeight="100vh"
    maxWidth="48rem"
    {...props}
  />
)
