import { Box } from "@chakra-ui/core"

export const Main = (props) => (
  <Box
    width="100%"
    py="2rem"
    px="2rem"
    justifyContent="center"
    minHeight="100vh"
    maxWidth="48rem"
    {...props}
  />
)
